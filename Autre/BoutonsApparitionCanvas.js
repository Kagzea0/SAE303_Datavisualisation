// LES INVESTISSEMENTS SNCF

document.getElementById('ButInvOpen').addEventListener('click', function() {
    var container = document.getElementById('Investissements');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

document.getElementById('ButInvClose').addEventListener('click', function() {
    var container = document.getElementById('Investissements');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

// LES COLLISIONS A UN PASSAGE A NIVEAU 

document.getElementById('ButColOpen').addEventListener('click', function() {
    var container = document.getElementById('Collisions');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

document.getElementById('ButColClose').addEventListener('click', function() {
    var container = document.getElementById('Collisions');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

// LES MOUVEMENTS ANNUELS DEPUIS 1947

document.getElementById('ButMvtAnnuelsOpen').addEventListener('click', function() {
    var container = document.getElementById('MvtAnnuels');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

document.getElementById('ButMvtAnnuelsClose').addEventListener('click', function() {
    var container = document.getElementById('MvtAnnuels');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

//  LES MOUVEMENTS MENSUELS DEPUIS 1994 

document.getElementById('ButMvtMensuelsOpen').addEventListener('click', function() {
    var container = document.getElementById('MvtMensuels');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

document.getElementById('ButMvtMensuelClose').addEventListener('click', function() {
    var container = document.getElementById('MvtMensuels');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

// LA REGULARITE MENSUELLE DES TGV DEPUIS 2015 

document.getElementById('ButRegulTGVOpen').addEventListener('click', function() {
    var container = document.getElementById('RegulariteTGV');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

document.getElementById('ButRegulTGVClose').addEventListener('click', function() {
    var container = document.getElementById('RegulariteTGV');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

// LE TEMPS DE TRAVAIL ANNUEL DEPUIS 1957

document.getElementById('ButTtaOpen').addEventListener('click', function() {
    var container = document.getElementById('TtaAnnuel1957');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

document.getElementById('ButTtaClose').addEventListener('click', function() {
    var container = document.getElementById('TtaAnnuel1957');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

// LE TRAFIC FRET ET VOYAGEUR DEPUIS 1841 

document.getElementById('ButTraficOpen').addEventListener('click', function() {
    var container = document.getElementById('Trafic');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});

document.getElementById('ButTraficClose').addEventListener('click', function() {
    var container = document.getElementById('Trafic');
    container.style.opacity = (container.style.opacity === '0') ? '1' : '0';
});